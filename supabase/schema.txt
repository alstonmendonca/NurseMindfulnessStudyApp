Table: participants

study_group (text)

department (text)

created_at (timestamptz)

last_active (timestamptz)

completed_onboarding (bool)

demographic_survey_completed (bool)

participant_number (int4) [PK?]

participant_password (text)

Table: mood_checks

id (int8) [PK]

participant_id (int4) [FK → participants.participant_number]

mood_score (int4)

stress_level (int4)

feelings (text)

note (text)

shift (text)

created_at (timestamptz)

Table: demographic_surveys

id (int4) [PK]

participant_id (int4) [FK → participants.participant_number]

sample_code (varchar)

age_group (varchar)

gender (varchar)

marital_status (varchar)

educational_qualification (varchar)

educational_other (text)

designation (varchar)

income_level (varchar)

years_experience (varchar)

working_unit (varchar)

working_unit_other (text)

work_shift (varchar)

hours_per_day (varchar)

night_shifts_per_month (varchar)

night_shifts_other (text)

place_of_residence (varchar)

residence_other (text)

contact_number (varchar)

created_at (timestamptz)

Table: journal_entries

id (int4) [PK]

participant_id (int4) [FK → participants.participant_number]

content (text)

prompt (text)

tags (text[])

shift (text)

created_at (timestamptz)

✅ Relationships:

participants ↔ mood_checks (1-to-many)

participants ↔ demographic_surveys (1-to-many)

participants ↔ journal_entries (1-to-many)